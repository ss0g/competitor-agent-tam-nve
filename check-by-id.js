require("dotenv").config(); const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function checkProjectById() { console.log("🔍 Checking project by user-provided ID\n"); const project = await prisma.project.findUnique({ where: { id: "cmbjv902z000zl8y6p89qi19e" }, include: { competitors: true, reports: true } }); if (!project) { console.log("❌ Project with ID cmbjv902z000zl8y6p89qi19e not found!"); } else { console.log(`✅ Found project: ${project.name} (${project.id})`); console.log(`Competitors: ${project.competitors.length}`); console.log(`Reports: ${project.reports.length}`); } await prisma.$disconnect(); } checkProjectById();
