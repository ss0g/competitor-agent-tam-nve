require("dotenv").config(); const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function test() { const allCompetitors = await prisma.competitor.findMany(); console.log(`Available Competitors: ${allCompetitors.length}`); const response = await fetch("http://localhost:3000/api/projects", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: "test-auto-060606", description: "Testing auto-assignment" }) }); const project = await response.json(); console.log(`Project: ${project.name}, Competitors: ${project.competitors.length}`); await prisma.$disconnect(); } test();
